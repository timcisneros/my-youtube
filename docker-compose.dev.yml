version: '3.8'

services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
      PORT: 3000
    volumes:
      - .:/app
      - /app/node_modules
      - app_data:/app/data
    command: ["node", "--watch", "server.js"]

volumes:
  app_data:
